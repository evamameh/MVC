{
    "name": "inventorycore/inventory-core",
    "description": "InventoryCore — custom PHP MVC (Core) + inventory MVP (App)",
    "type": "project",
    "license": "proprietary",
    "require": {
        "php": ">=8.3"
    },
    "autoload": {
        "psr-4": {
            "Core\\": "core/",
            "App\\": "app/"
        }
    },
    "config": {
        "sort-packages": true
    }
}
